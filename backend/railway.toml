# Railway configuration
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Temporarily disabled healthcheck to debug why the app is slow to respond
# healthcheckPath = "/api/health"
# healthcheckTimeout = 300
